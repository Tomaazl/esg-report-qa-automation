# Requirements for Document Question Parser
# Core dependencies
docling>=1.0.0
pathlib2>=2.3.0  # For older Python versions, built-in for Python 3.4+

# Optional dependencies for enhanced functionality
pandas>=1.5.0  # For data manipulation if needed
openpyxl>=3.0.0  # For Excel file handling
python-docx>=0.8.11  # For direct DOCX handling if needed
pypdf2>=3.0.0  # For direct PDF handling if needed

# Development dependencies (optional)
pytest>=7.0.0  # For testing
black>=22.0.0  # For code formatting
flake8>=5.0.0  # For linting
